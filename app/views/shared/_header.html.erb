<header>
  <h1><%= link_to 'Should I Send This?', root_url %></h1>
  <nav>
    <ul>
      <% if current_user %>
        <li> <%= link_to current_user.name, user_path(current_user) %> </li>
        <li> <%= link_to 'New', new_document_path %>   </li>
        <li> <%= link_to 'Show All', documents_path %>   </li>
      <% end %>
      <li><%= render 'devise/menu/registration_items' %></li>
      <li><%= render 'devise/menu/login_items' %></li>
    </ul>
  </nav>
</header>
